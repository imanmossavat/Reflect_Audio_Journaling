# Patterns

Regex-based patterns for detecting structured PII such as:
- Email addresses
- Phone numbers
- Postal codes

These patterns complement model-based detection and improve recall
for well-defined identifiers.
